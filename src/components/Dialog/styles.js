import { StyleSheet } from 'react-native'


const styles = StyleSheet.create({

    container: {
        borderRadius: 80
    },

    title: {

        fontSize: 25,
        color: 'red'
    },

    name: {
        fontWeight: 'bold',
        fontSize: 18,
        color: 'black'
    },

    texto: {
        fontWeight: 'bold',
        fontSize: 15,
        color: 'gray'
    },

    button: {
        fontWeight: "bold",
        fontSize: 28,
        color: "green"
    }

})

export default styles