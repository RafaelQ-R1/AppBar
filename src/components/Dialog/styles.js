import { StyleSheet } from 'react-native'


const styles = StyleSheet.create({

    container: {
        borderRadius: 10,

    },

    title: {

        fontSize: 25,
        color: 'red',
        textShadowRadius: 40,

    },

    name: {
        fontWeight: 'bold',
        fontSize: 18,
        color: 'black'
    },

    texto: {
        fontWeight: 'bold',
        fontSize: 15,
        color: 'gray'
    },

    button: {
        fontWeight: "bold",
        fontSize: 28,
        color: "green"
    }

})

export default styles